import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Quadratic Funding - The International Philanthropy",
  description: "Utilize quadratic funding to boost the amount being donated to specific sections. Whether it's for individuals or animals, we will raise many rounds to satisfy all donations",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
